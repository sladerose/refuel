---
phase: 08-proactive-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [refuel/ProactiveService.swift, refuel/NotificationManager.swift, refuel/MapView.swift, refuel/StationListView.swift, refuel/Models.swift]
autonomous: true
requirements: [PROACTIVE-01, PROACTIVE-02, PROACTIVE-03, PROACTIVE-04]
must_haves:
  truths:
    - "User is prompted to verify prices after staying at a station for 2 minutes."
    - "User receives a 'Forget to scan?' notification 10 minutes after leaving a station without contributing."
    - "Hike alerts appear 24 hours before the first Wednesday of the month."
    - "Map/List views show a 'Beat the Hike' countdown 48 hours before price changes."
  artifacts:
    - path: "refuel/NotificationManager.swift"
      provides: "Local notification scheduling and permission handling"
    - path: "refuel/ProactiveService.swift"
      provides: "Dwell detection and hike prediction logic"
  key_links:
    - from: "refuel/ProactiveService.swift"
      to: "refuel/GeofenceService.swift"
      via: "Monitoring station entries/exits"
---

<objective>
Implement proactive features that nudge users toward data contribution based on location (dwell detection) and external timing (regulated price hikes).

Purpose: Increase data freshness and user engagement through timely, relevant notifications.
Output: ProactiveService, NotificationManager, and updated UI indicators.
</objective>

<execution_context>
@refuel/GeofenceService.swift
@refuel/LocationManager.swift
@refuel/Models.swift
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification Infrastructure & Permissions</name>
  <files>refuel/NotificationManager.swift, refuel/refuelApp.swift</files>
  <action>
    - Create `NotificationManager` using `UNUserNotificationCenter`.
    - Implement `requestPermissions()` and check authorization status.
    - Add a method to schedule local notifications with custom identifiers (dwell, follow-up, hike).
    - Handle notification clicks: Implement `userNotificationCenter(_:didReceive:withCompletionHandler:)` to set an app-wide `activeRoute` or `selectedStationID` that triggers navigation to `PriceVerificationView` or `ReceiptScannerView`.
  </action>
  <verify>
    <automated>Request permissions in a test harness; verify scheduling command returns successfully.</automated>
  </verify>
  <done>App requests notification permissions on launch; infrastructure for scheduling and deep linking is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Dwell Detection & Follow-up Logic</name>
  <files>refuel/ProactiveService.swift, refuel/GeofenceService.swift</files>
  <action>
    - Create `ProactiveService` to coordinate location events and notifications.
    - Extend `GeofenceService` to notify `ProactiveService` on region entry and exit.
    - Dwell Logic: On entry to a station region, start a 2-minute timer. If the user is still in the region after 2 minutes, trigger "Still correct?" notification.
    - Exit Logic: On exit, if no contribution (XP gain or RefuelEvent) was recorded in the last 15 minutes, schedule a "Forget to scan?" notification for 10 minutes in the future.
    - Ensure timers are invalidated if the user leaves early.
  </action>
  <verify>
    <automated>Unit test ProactiveService by mocking entry/exit events and verifying notification scheduling calls.</automated>
  </verify>
  <done>User receives timely notifications based on dwell time and station departure.</done>
</task>

<task type="auto">
  <name>Task 3: Hike Alerts & 'Beat the Hike' UI</name>
  <files>refuel/ProactiveService.swift, refuel/MapView.swift, refuel/StationListView.swift</files>
  <action>
    - Hike Prediction: In `ProactiveService`, implement a helper to calculate the "First Wednesday of the Month" (SA fuel price change day).
    - Schedule "Hike Alert" notification 24 hours before this date (using mock CEF data indicating a price rise).
    - UI Implementation: Add a `HikeAlertBanner` component.
    - Display the banner in `MapView` and `StationListView` starting 48 hours before the change date. Include a countdown (e.g., "Price hike in 34h 12m").
    - Use a persistent state flag `isHikeImminent` to drive UI visibility.
  </action>
  <verify>
    <automated>Mock current date to 47 hours before the first Wednesday and verify banner presence in SwiftUI previews/tests.</automated>
  </verify>
  <done>Predictive hike alerts are scheduled monthly and UI provides visual urgency during hike windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System → App | Notification responses (deep links) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | S | Deep Link | Mitigate | Validate station IDs passed in notification userInfo before navigating. |
| T-08-02 | R | Notifications | Mitigate | Prevent notification spam by checking `lastNotificationDate` in ProactiveService. |
</threat_model>

<success_criteria>
- App requests and receives notification permissions.
- Dwelling at a station (simulated) for 2 minutes triggers a verification prompt.
- Leaving a station triggers a follow-up if no scan occurred.
- "Beat the Hike" banner appears correctly based on the monthly calendar.
</success_criteria>

<output>
After completion, create `.planning/phases/08-proactive-intelligence/08-01-SUMMARY.md`
</output>
