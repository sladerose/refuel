---
phase: 11-the-cloud-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLOUDKIT_SETUP.md
  - refuel/refuelApp.swift
autonomous: false
requirements: [PHASE-11]
user_setup:
  - service: apple_developer
    why: "CloudKit capabilities require an active Apple Developer account and manual Xcode configuration"
    env_vars: []
    dashboard_config:
      - task: "Enable iCloud capability (CloudKit, iCloud.com.refuel.app container)"
        location: "Xcode -> refuel Target -> Signing & Capabilities"
      - task: "Enable Background Modes capability (Remote notifications)"
        location: "Xcode -> refuel Target -> Signing & Capabilities"

must_haves:
  truths:
    - Developer has clear instructions for manual Xcode entitlement setup
    - The app's SwiftData container is configured to sync with CloudKit
    - Personal data correctly syncs across multiple devices via the private database
  artifacts:
    - path: CLOUDKIT_SETUP.md
      provides: "Exact manual setup steps for developers"
    - path: refuel/refuelApp.swift
      provides: "Restored cloudKitDatabase configuration"
  key_links:
    - from: refuel/refuelApp.swift
      to: iCloud.com.refuel.app
      via: "ModelConfiguration.cloudKitDatabase"
      pattern: "cloudKitDatabase"
---

<objective>
Resolve Xcode entitlement blockers and fully activate CloudKit for SwiftData syncing.

Purpose: Enable real-time cloud data sync of personal data (Favorites, Refuel History) across multiple user devices using the private iCloud database.
Output: Developer documentation for Xcode setup, updated SwiftData configuration, and end-to-end sync validation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-vNext.md
@.planning/STATE.md
@refuel/refuelApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Xcode CloudKit Setup</name>
  <files>CLOUDKIT_SETUP.md</files>
  <action>Create a step-by-step markdown guide for developers detailing how to manually configure Xcode entitlements. Must include instructions to add the 'iCloud' capability (checking 'CloudKit' and adding the 'iCloud.com.refuel.app' container) and the 'Background Modes' capability (checking 'Remote notifications').</action>
  <verify>
    <automated>test -f CLOUDKIT_SETUP.md</automated>
  </verify>
  <done>Documentation file exists with precise Xcode capability instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Restore cloudKitDatabase Configuration</name>
  <files>refuel/refuelApp.swift</files>
  <action>Modify `ModelConfiguration` initialization in `sharedModelContainer` to explicitly set `cloudKitDatabase: .automatic` to restore SwiftData CloudKit sync capability. Ensure it does not break existing schema configuration.</action>
  <verify>
    <automated>grep -q "cloudKitDatabase" refuel/refuelApp.swift</automated>
  </verify>
  <done>ModelConfiguration includes the cloudKitDatabase parameter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate End-to-End Sync</name>
  <files>None</files>
  <action>Pause execution for the user to manually verify the setup and CloudKit sync.</action>
  <what-built>CloudKit Setup Documentation and SwiftData Configuration</what-built>
  <how-to-verify>
    1. Follow the steps in CLOUDKIT_SETUP.md to manually configure Xcode entitlements (iCloud, Background Modes).
    2. Build and run the app on a physical device or Simulator (Device A).
    3. Create a test record (e.g., Favorite a station or add a Refuel Event).
    4. Build and run the app on a second device/Simulator (Device B) logged into the same Apple ID.
    5. Verify that the test record appears on Device B.
  </how-to-verify>
  <verify>
    <automated>MISSING — Manual human verification required</automated>
  </verify>
  <resume-signal>Type "approved" if sync is successful, or describe any errors encountered.</resume-signal>
  <done>Sync is confirmed working across two devices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device -> iCloud | Personal data synced to Apple's CloudKit private database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | CloudKit Sync | accept | Data is synced to the user's private iCloud database, which is secured by Apple's infrastructure. No public data sharing in this phase. |
| T-11-02 | Tampering | SwiftData | mitigate | Rely on SwiftData and CloudKit's built-in conflict resolution and data validation. |
</threat_model>

<verification>
- CLOUDKIT_SETUP.md contains accurate capability setup instructions.
- `refuelApp.swift` compiles cleanly with the updated `ModelConfiguration`.
- End-to-end sync is verified via human checkpoint.
</verification>

<success_criteria>
- App successfully syncs user data to the `iCloud.com.refuel.app` private database.
- Data changes on one device automatically propagate to other devices on the same Apple ID.
</success_criteria>

<output>
After completion, create `.planning/phases/11-the-cloud-foundation/11-01-SUMMARY.md`
</output>
