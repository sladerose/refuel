---
phase: 03-value-analytics
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [refuel/GeofenceService.swift, refuel/FuelPriceIngestor.swift, refuel/refuelApp.swift]
autonomous: true
requirements: [DISCO-03]
must_haves:
  truths:
    - "App automatically triggers a price refresh when the user moves significantly (e.g., 5km)."
    - "Background geofence monitoring is active even when the app is in the background (using CLMonitor)."
  artifacts:
    - path: "refuel/GeofenceService.swift"
      provides: "iOS 17+ CLMonitor management for dynamic search regions."
  key_links:
    - from: "refuel/GeofenceService.swift"
      to: "refuel/FuelPriceIngestor.swift"
      via: "triggers updatePrices() on geofence exit"
---

<objective>
Implement dynamic geofencing using CLMonitor to ensure fuel price data stays fresh as the user moves between locations.

Purpose: Automate data refreshes to maintain accuracy without requiring manual user interaction (DISCO-03).
Output: A new GeofenceService and integration with the FuelPriceIngestor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-value-analytics/03-RESEARCH.md
@.planning/phases/03-value-analytics/03-01-SUMMARY.md

# Services for integration
@refuel/FuelPriceIngestor.swift
@refuel/refuelApp.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GeofenceService using CLMonitor</name>
  <files>refuel/GeofenceService.swift, refuelTests/GeofenceTests.swift</files>
  <behavior>
    - GeofenceService should use `CLMonitor` to track a circular condition (center + radius).
    - It should expose an async stream or delegate-like mechanism to notify when the user exits the current geofence.
    - It should handle the `satisfied` -> `unsatisfied` transition for a given region identifier.
  </behavior>
  <action>
    1. Create `refuel/GeofenceService.swift`:
       - Use `import CoreLocation`.
       - Implement `actor GeofenceService`.
       - Use `CLMonitor` to add a circular condition (e.g., 5km radius).
       - Listen for `events` and trigger a callback/closure when an exit occurs.
    2. Create `refuelTests/GeofenceTests.swift` to mock or verify monitoring logic where possible (simulating events if the API allows).
  </action>
  <verify>
    <automated>swift test --filter GeofenceTests</automated>
  </verify>
  <done>GeofenceService correctly monitors regions and detects exits.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate GeofenceService with FuelPriceIngestor</name>
  <files>refuel/FuelPriceIngestor.swift, refuel/refuelApp.swift</files>
  <action>
    1. Update `refuelApp.swift` (or a central Coordinator):
       - Initialize `GeofenceService`.
       - When a geofence exit is detected:
         - Identify the new center (current location).
         - Call `FuelPriceIngestor.updatePrices(latitude:longitude:service:)`.
         - Update the geofence condition to the new center.
    2. Ensure `FuelPriceIngestor` is prepared to be called from the background if needed.
  </action>
  <verify>
    <automated># Compilation check and service wiring verification
    grep -r "GeofenceService" refuel/
    </automated>
  </verify>
  <done>Geofence events successfully trigger data refreshes.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Dynamic Geofencing</what-built>
  <how-to-verify>
    1. Using the iOS Simulator:
       - Open the app and let it load stations.
       - Change the simulator location (Features -> Location -> Custom Location) to a point > 5km away.
    2. Observe the app: It should trigger a new network fetch and refresh the map/list with new stations.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoreLocation | Receiving location updates from the OS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Privacy | GeofenceService | mitigate | Ensure "Always" location permission is requested and justified for background refreshes. Minimize data collection to only what's needed for the geofence. |
| T-03-04 | Information Disclosure | Location State | accept | Location center is used locally to fetch prices; not sent to third parties other than the price API provider. |
</threat_model>

<success_criteria>
- CLMonitor successfully detects region exits.
- Exit events trigger new data fetches from the FuelPriceIngestor.
- The search region updates dynamically as the user travels.
</success_criteria>

<output>
After completion, create `.planning/phases/03-value-analytics/03-02-SUMMARY.md`
</output>
