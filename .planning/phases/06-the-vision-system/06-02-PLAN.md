---
phase: 06-the-vision-system
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified: [refuel/OCRService.swift, refuel/StationDetailView.swift, refuel/StationListView.swift, refuel/PriceBoardScannerView.swift, refuel/PriceVerificationView.swift]
autonomous: true
requirements: [VISION-02]

must_haves:
  truths:
    - "User can launch a live price board scanner from StationDetailView and StationListView"
    - "Scanner identifies fuel grades and corresponding prices in real-time"
    - "Verification UI displays detected prices for review before saving"
    - "Confirmed prices update the station's fuel grades in SwiftData"
  artifacts:
    - path: "refuel/PriceBoardScannerView.swift"
      provides: "Live OCR camera interface using DataScannerViewController"
    - path: "refuel/PriceVerificationView.swift"
      provides: "Confirmation UI for detected prices"
    - path: "refuel/OCRService.swift"
      provides: "Price board pattern recognition logic"
  key_links:
    - from: "refuel/StationListView.swift"
      to: "refuel/PriceBoardScannerView.swift"
      via: "Swipe action"
    - from: "refuel/PriceBoardScannerView.swift"
      to: "refuel/PriceVerificationView.swift"
      via: "Scanner completion callback"
    - from: "refuel/PriceVerificationView.swift"
      to: "SwiftData Station.prices"
      via: "ModelContext update"
---

<objective>
Implement camera-based Price Board Recognition to allow users to update fuel prices by scanning station signage. This plan covers the live OCR interface, pattern recognition for outdoor signs, and the verification workflow to ensure data accuracy.

Purpose: Eliminate manual entry friction for price updates and improve data freshness.
Output: Live scanner UI, price board parsing logic, and a verification/confirmation flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-the-vision-system/06-01-SUMMARY.md
@refuel/OCRService.swift
@refuel/Models.swift
@refuel/StationDetailView.swift
@refuel/StationListView.swift

<interfaces>
From refuel/OCRService.swift:
```typescript
class OCRService {
    static let shared: OCRService
    func process(images: [UIImage], stations: [Station], completion: @escaping (ScannedReceiptData) -> Void)
    internal func extractDecimal(from text: String) -> Double?
}
```

From refuel/Models.swift:
```typescript
@Model final class Station {
    var name: String
    var prices: [FuelPrice]
    var lastUpdated: Date?
}

@Model final class FuelPrice {
    var grade: String
    var price: Double
    var station: Station?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OCRService and Create PriceBoardScannerView</name>
  <files>refuel/OCRService.swift, refuel/PriceBoardScannerView.swift</files>
  <action>
    1. Update `OCRService` to include a method for parsing price board text strings. It should look for fuel grade keywords (91, 93, 95, 98, Diesel, ULP) and attempt to find the nearest decimal value (X.XX) in the same or adjacent lines.
    2. Create `PriceBoardScannerView` using `DataScannerViewController` (VisionKit). 
    3. Configure the scanner to recognize text and provide live feedback (bounding boxes) on detected items.
    4. Implement a "Capture" mechanism that aggregates all visible text in the scanner's view and passes it to `OCRService` for final parsing into a dictionary of `[Grade: Price]`.
  </action>
  <verify>
    <automated>Check that PriceBoardScannerView compiles and OCRService has a testable method for price board parsing.</automated>
  </verify>
  <done>Price board recognition logic implemented and live scanner interface created.</done>
</task>

<task type="auto">
  <name>Task 2: Create PriceVerificationView and Model Integration</name>
  <files>refuel/PriceVerificationView.swift</files>
  <action>
    1. Create `PriceVerificationView` which accepts a `Station` and a dictionary of detected `[Grade: Price]`.
    2. Display the detected prices in a list where each entry can be edited (in case of OCR errors).
    3. Implement a "Confirm & Update" button that:
       - For each confirmed grade, updates the existing `FuelPrice` record for the station or creates a new one if it doesn't exist.
       - Updates the station's `lastUpdated` timestamp.
       - Triggers an update to the station's `zScore` (via existing analytics service if applicable, or mark for refresh).
       - Dismisses the view and returns to the previous screen.
  </action>
  <verify>
    <automated>Verify that PriceVerificationView can be initialized with mock data and correctly updates a mock Station's prices in a SwiftData model context.</automated>
  </verify>
  <done>Verification UI allows users to review and commit prices to the database.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Scan Actions into UI</name>
  <files>refuel/StationDetailView.swift, refuel/StationListView.swift</files>
  <action>
    1. In `StationDetailView`, add a second camera button labeled "Scan Price Board" (per D-03).
    2. Wire this button to present `PriceBoardScannerView`. On completion, present `PriceVerificationView` with the results.
    3. In `StationListView`, add a `.swipeActions(edge: .trailing)` to each station row with a "Scan" button (orange color) that directly opens the scanner for that station.
    4. Ensure proper navigation and sheet management so the user flows naturally from scan to verification to the updated view.
  </action>
  <verify>
    <automated>Check that StationDetailView and StationListView now contain buttons/swipe actions for Price Board scanning.</automated>
  </verify>
  <done>UI entry points for price board recognition are fully integrated into Map and List flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Database | OCR results are untrusted and must be verified by the user before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | SwiftData (Station Prices) | mitigate | Implement mandatory user verification UI (Task 2) to prevent incorrect OCR data from automatically corrupting the price database. |
| T-06-02 | Spoofing | Camera Input | accept | Users could photograph fake signs, but this is a community-contributed app; verification UI handles unintentional errors, while intentional bad data is out of scope for local OCR logic. |
</threat_model>

<verification>
1. Launch app and navigate to a Station Detail view.
2. Tap "Scan Price Board" and verify camera opens with live OCR overlays.
3. Simulate a scan (or use real signage) and verify detection of grades (95, Diesel, etc.).
4. Confirm detected prices appear in the Verification UI.
5. Modify one price and tap "Confirm".
6. Verify the Station Detail view now shows the updated prices and "last updated" time is current.
7. Repeat via swipe action in StationListView.
</verification>

<success_criteria>
- Users can scan a price board and have grades/prices extracted with high accuracy.
- Mandatory verification step prevents automatic data corruption.
- Prices are successfully persisted in SwiftData and reflect in UI immediately.
- Entry points exist in both Map (via Detail sheet) and List views.
</success_criteria>

<output>
After completion, create `.planning/phases/06-the-vision-system/06-02-SUMMARY.md`
</output>
