---
phase: 06-the-vision-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [refuel.xcodeproj/project.pbxproj, refuel/ReceiptScannerView.swift, refuel/OCRService.swift, refuel/ContentView.swift, refuel/StationDetailView.swift]
autonomous: true
requirements: [VISION-01, VISION-02]

must_haves:
  truths:
    - "User can trigger a receipt scan from the History view"
    - "User can trigger a receipt scan from the Station Detail view"
    - "The app extracts station name, date, volume, and total cost from a scanned receipt"
    - "A new RefuelEvent is created with the scanned data"
    - "The station's FuelPrice for the scanned grade is updated"
  artifacts:
    - path: "refuel/ReceiptScannerView.swift"
      provides: "VisionKit document camera bridge"
    - path: "refuel/OCRService.swift"
      provides: "Text recognition and field extraction logic"
  key_links:
    - from: "refuel/ReceiptScannerView.swift"
      to: "refuel/OCRService.swift"
      via: "delegate callback with images"
    - from: "refuel/OCRService.swift"
      to: "refuel/ContentView.swift"
      via: "populated AddRefuelLogView"
---

<objective>
Integrate VisionKit to allow users to scan fuel receipts, extract key data points (Station, Date, Grade, Volume, Cost), and automatically log the refuel event while updating station prices.

Purpose: Reduce friction in data entry for cost tracking and price crowd-sourcing.
Output: Integrated OCR scanning flow in History and Detail views.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@refuel/Models.swift
@refuel/ContentView.swift
@refuel/StationDetailView.swift

<interfaces>
From refuel/Models.swift:
```swift
@Model
final class Station {
    var name: String
    var prices: [FuelPrice] = []
    var refuelLogs: [RefuelEvent] = []
}

@Model
final class FuelPrice {
    var grade: String
    var price: Double
    var station: Station?
}

@Model
final class RefuelEvent {
    var date: Date
    var amountInLitres: Double
    var pricePerLitre: Double
    var grade: String
    var stationName: String
    var station: Station?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vision Infrastructure & Permissions</name>
  <files>refuel.xcodeproj/project.pbxproj, refuel/ReceiptScannerView.swift, refuel/OCRService.swift</files>
  <action>
    1. Add `NSCameraUsageDescription` to `project.pbxproj` (Value: "Refuel needs camera access to scan receipts and price boards."). Use `INFOPLIST_KEY_NSCameraUsageDescription` in build settings if applicable.
    2. Create `ReceiptScannerView.swift` implementing `UIViewControllerRepresentable` for `VNDocumentCameraViewController`. Provide a coordinator to handle `didFinishWithScan` and `didFailWithError`.
    3. Create `OCRService.swift` using `VNImageRequestHandler` and `VNRecognizeTextRequest`. Set `recognitionLevel` to `.accurate`.
  </action>
  <verify>
    <automated>grep "NSCameraUsageDescription" refuel.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>Infrastructure for scanning and text recognition is ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Field Extraction & Data Logic</name>
  <files>refuel/OCRService.swift, refuelTests/OCRServiceTests.swift</files>
  <behavior>
    - OCRService should identify "Total" or "Amount" and extract the following decimal value.
    - OCRService should identify "Litres", "Volume", or "Qty" and extract the following decimal value.
    - OCRService should identify fuel grades (91, 95, Diesel) from keywords.
    - OCRService should attempt to fuzzy-match station name against provided Station list.
  </behavior>
  <action>
    Implement text parsing using regular expressions and keyword matching in `OCRService.process(images: [UIImage], stations: [Station])`. 
    Extract: stationName, date, amountInLitres, pricePerLitre, grade.
  </action>
  <verify>
    <automated>swift test --filter OCRServiceTests</automated>
  </verify>
  <done>OCR logic correctly parses mock receipt text into structured data.</done>
</task>

<task type="auto">
  <name>Task 3: UI Integration & Auto-logging</name>
  <files>refuel/ContentView.swift, refuel/StationDetailView.swift</files>
  <action>
    1. Update `RefuelHistoryView` in `ContentView.swift`:
       - Add a `camera` button to the toolbar.
       - Present `ReceiptScannerView` in a sheet.
       - On success, pass images to `OCRService`, then pre-populate `AddRefuelLogView`.
    2. Update `StationDetailView.swift`:
       - Add a "Scan Receipt" button in the "Navigation Buttons" section (or above it).
       - Pre-set the `selectedStation` in the scanner result.
    3. Update `AddRefuelLogView` to support external data injection (e.g., via a binding or initializer).
    4. Ensure saving a log also updates the station's `FuelPrice` for that grade if the station matches.
  </action>
  <verify>
    <automated>grep -l "ReceiptScannerView" refuel/ContentView.swift refuel/StationDetailView.swift</automated>
  </verify>
  <done>Scanning flow is accessible from UI and correctly populates the logging form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Camera → VisionKit | Scanned images are processed locally via VisionKit. |
| VisionKit → App Logic | Extracted text is parsed and converted to Model objects. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Camera Buffer | mitigate | All image processing happens in-memory and locally; images are not persisted or sent to any server. |
| T-06-02 | Tampering | OCR Data | accept | User must verify and confirm OCR results in the AddRefuelLogView before persistence. |
</threat_model>

<verification>
1. Open History view -> Tap Camera -> Scan a receipt -> Confirm data is pre-populated in form -> Save -> Verify log appears in list.
2. Open Station Detail -> Tap Scan Receipt -> Scan receipt -> Verify station is already selected and other fields are populated.
3. Check that the station's price in Detail view reflects the scanned price if it was updated.
</verification>

<success_criteria>
1. User can successfully scan a receipt and see populated fields.
2. At least Station Name and Total Cost are correctly identified for high-contrast receipts.
3. FuelPrice updates in the database after a successful scan and save.
</success_criteria>

<output>
After completion, create `.planning/phases/06-the-vision-system/06-01-PLAN-SUMMARY.md`
</output>
