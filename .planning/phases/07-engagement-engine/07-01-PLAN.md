---
phase: 07-engagement-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [refuel/Models.swift, refuel/refuelApp.swift, refuel/GamificationManager.swift, refuel/ContentView.swift, refuel/ProfileView.swift, refuel/GamificationViews.swift, refuel/PriceVerificationView.swift, refuel/StationListView.swift, refuel/StationDetailView.swift]
autonomous: true
requirements: [ENGAGE-01, ENGAGE-02, ENGAGE-03]
must_haves:
  truths:
    - "User stats (XP, Rank, Streak) persist across app launches."
    - "Contributing (scanning/verifying) awards XP and updates streaks correctly."
    - "Profile tab displays current stats and community impact."
    - "Streak indicator is visible on Map and List views."
  artifacts:
    - path: "refuel/Models.swift"
      provides: "UserProfile model"
    - path: "refuel/GamificationManager.swift"
      provides: "XP and Streak logic"
    - path: "refuel/ProfileView.swift"
      provides: "User profile dashboard"
  key_links:
    - from: "refuel/PriceVerificationView.swift"
      to: "refuel/GamificationManager.swift"
      via: "awardXP call"
    - from: "refuel/ContentView.swift"
      to: "refuel/ProfileView.swift"
      via: "TabView integration"

---

<objective>
Establish the gamification engine for Refuel, including persistent user stats, Duolingo-style streak logic (10-day window), and XP rewards for community contributions.

Purpose: Increase user retention and data quality by rewarding frequent contributions.
Output: Persistent UserProfile model, GamificationManager logic, Profile dashboard tab, and Streak indicators.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@refuel/Models.swift
@refuel/ContentView.swift
@refuel/StationListView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundation - UserProfile Model & GamificationManager</name>
  <files>refuel/Models.swift, refuel/refuelApp.swift, refuel/GamificationManager.swift</files>
  <action>
    - Add `UserProfile` SwiftData model to `Models.swift` with fields: `xp` (Int), `streakCount` (Int), `lastContributionDate` (Date?), `communityImpact` (Double).
    - Implement a `rank` computed property in `UserProfile` (Newcomer < 500 XP, Active Member < 1000, Reliable Contributor < 2500, Expert Scout < 5000, Fuel Legend >= 5000).
    - Update `refuelApp.swift` to include `UserProfile.self` in the `ModelContainer` schema.
    - Create `GamificationManager.swift` as an `@Observable` class that:
      - Fetches or creates a singleton `UserProfile`.
      - Provides `awardXP(amount: Int)` which increments XP and updates the streak.
      - Implements streak logic: Increment streak if contribution is on a new day within 10 days of the last; reset to 1 if > 10 days; set to 1 if first contribution.
      - Provides `addCommunityImpact(savings: Double)` to track Rand saved for others.
  </action>
  <verify>
    <automated>Check that UserProfile is in schema and GamificationManager initializes without error.</automated>
  </verify>
  <done>UserProfile model persisted and GamificationManager logic implemented with 10-day streak window.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring - XP Award System Integration</name>
  <files>refuel/ContentView.swift, refuel/PriceVerificationView.swift, refuel/StationListView.swift, refuel/StationDetailView.swift</files>
  <action>
    - Update `ContentView.swift` to initialize `GamificationManager` and provide it via environment: `.environment(gamificationManager)`.
    - Award XP in `AddRefuelLogView.saveLog()`:
      - +50 XP if `initialData` (receipt scan) is present.
      - +10 XP for the manual entry/verification aspect.
    - Award XP in `PriceVerificationView.savePrices()`:
      - +10 XP for price verification.
    - Award XP in `StationListView.swift` and `StationDetailView.swift` scanners:
      - +30 XP immediately when a Price Board Scan is successfully captured (before verification).
    - Ensure all awards trigger `gamificationManager.awardXP()`.
  </action>
  <verify>
    <automated>Trigger a mock scan or verification and check if XP increments in the debugger or logs.</automated>
  </verify>
  <done>XP rewards wired into receipt scans (50), board scans (30), and verifications (10).</done>
</task>

<task type="auto">
  <name>Task 3: UI - Profile Tab & Streak Indicators</name>
  <files>refuel/ProfileView.swift, refuel/GamificationViews.swift, refuel/ContentView.swift, refuel/MapView.swift, refuel/StationListView.swift</files>
  <action>
    - Create `ProfileView.swift` to display user stats: Rank (with progress bar to next level), XP total, Streak count, and Community Impact (Rand saved).
    - Create `GamificationViews.swift` containing a `StreakIndicator` (a small flame/pill UI showing the current streak).
    - Update `ContentView.swift` TabView to include a "Profile" tab with `person.circle.fill` icon.
    - Add `StreakIndicator` as a top-right overlay in `MapView.swift`.
    - Add `StreakIndicator` to the header area of `StationListView.swift`.
  </action>
  <verify>
    <automated>UI check: Profile tab appears in navigation; Streak indicator visible on Map and List.</automated>
  </verify>
  <done>Gamification UI complete with Profile dashboard and streak visibility across main views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> XP Logic | User actions trigger XP awards; logic is client-side but relies on persisted state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | UserProfile | accept | Persistence is local (SwiftData). While users could theoretically edit the DB on a rooted device, the value is personal gamification, not financial. |
| T-07-01-02 | Repudiation | XP Awarding | mitigate | Log XP awards to console for debugging; ensure `modelContext.save()` is called after awards. |
</threat_model>

<verification>
1. Launch app and verify "Profile" tab exists.
2. Complete a "Price Verification" and check that XP increases from 0 to 10.
3. Verify "Streak" increases to 1.
4. Check Profile view to ensure Rank is "Newcomer" and XP/Impact are displayed correctly.
</verification>

<success_criteria>
- UserProfile SwiftData model persists XP, Rank, and Streaks.
- GamificationManager correctly handles 10-day streak grace period.
- XP awarded: 50 (Receipt), 30 (Board Scan), 10 (Verification).
- Profile tab displays stats and community impact.
- Streak indicator visible on Map and List views.
</success_criteria>

<output>
After completion, create `.planning/phases/07-engagement-engine/07-01-SUMMARY.md`
</output>
