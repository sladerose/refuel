---
phase: 05-project-polish
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified: [refuel/ContentView.swift, refuel/Models.swift, refuel/StationListView.swift, refuel/MapView.swift, .planning/ROADMAP.md]
autonomous: true
requirements: [DISCO-01, DISCO-02, INFO-02, TRACK-01, TRACK-02]

must_haves:
  truths:
    - "User sees a ProgressView overlay when the app is refreshing prices"
    - "RAG status colors have sufficient contrast (Average status is readable)"
    - "Station list and refuel history support pull-to-refresh"
    - "Favorite buttons and map markers have descriptive accessibility labels"
  artifacts:
    - path: "refuel/ContentView.swift"
      provides: "Loading feedback and pull-to-refresh wiring"
    - path: "refuel/Models.swift"
      provides: "Improved color contrast for RAG status"
    - path: "refuel/StationListView.swift"
      provides: "Pull-to-refresh and accessibility for list items"
    - path: "refuel/MapView.swift"
      provides: "Accessibility for map annotations"

  key_links:
    - from: "refuel/ContentView.swift"
      to: "refuel/StationListView.swift"
      via: "refreshPrices closure"
    - from: "refuel/Models.swift"
      to: "refuel/MapView.swift"
      via: "ragStatus.color"
---

<objective>
Address top UI/UX and accessibility findings from the phase review. This includes adding loading feedback, improving color contrast for better readability, implementing pull-to-refresh for manual data updates, and enhancing accessibility labels for screen readers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-polish/05-UI-REVIEW.md
@refuel/ContentView.swift
@refuel/Models.swift
@refuel/StationListView.swift
@refuel/MapView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve Feedback and Color Contrast</name>
  <files>refuel/Models.swift, refuel/ContentView.swift</files>
  <action>
    - In `refuel/Models.swift`:
        - Update `RAGStatus.average.color` to use a darker amber/orange for better contrast with white text. Use `Color(red: 1.0, green: 0.65, blue: 0.0)` or similar.
        - Ensure `RAGStatus.expensive.color` remains distinct (e.g., standard `.orange` or a slightly deeper red-orange).
    - In `refuel/ContentView.swift`:
        - Add a `ProgressView` overlay to the `TabView`. 
        - The overlay should appear only when `isRefreshing` is true, featuring a background material (e.g., `.ultraThinMaterial`) and a label like "Updating Prices...".
  </action>
  <verify>
    - Verify `isRefreshing` state triggers the overlay.
    - Check the new amber color in a preview.
  </verify>
  <done>
    Users get visual feedback during background refreshes, and the "Average" price status is clearly readable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Pull-to-Refresh</name>
  <files>refuel/ContentView.swift, refuel/StationListView.swift</files>
  <action>
    - In `refuel/StationListView.swift`:
        - Add an `onRefresh: (() async -> Void)? = nil` parameter to the initializer.
        - Add the `.refreshable` modifier to the `List` (or `ScrollView`), calling `onRefresh?()` if it exists.
    - In `refuel/ContentView.swift`:
        - Pass the `refreshPrices` function (as a closure) to both instances of `StationListView`.
        - Add the `.refreshable` modifier to the `List` in `RefuelHistoryView` (located at the bottom of the file), also calling `refreshPrices()`.
  </action>
  <verify>
    Pulling down on the station list or history list triggers the `isRefreshing` state and the overlay.
  </verify>
  <done>
    Users can manually trigger data updates from any primary list view.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance Accessibility</name>
  <files>refuel/StationListView.swift, refuel/MapView.swift</files>
  <action>
    - In `refuel/StationListView.swift` (StationRow):
        - Add `.accessibilityLabel(station.isFavorite ? "Remove from Favorites" : "Add to Favorites")` to the favorite button.
        - Add `.accessibilityValue(station.isFavorite ? "Favorited" : "Not Favorited")`.
    - In `refuel/MapView.swift`:
        - Add `.accessibilityLabel("\(station.name), \(station.ragStatus.rawValue) value")` to the station `Annotation`.
        - Add `.accessibilityAction(named: "Toggle Favorite") { station.isFavorite.toggle() }` to the station annotation.
        - Ensure the favorite button inside the annotation also has appropriate labels.
  </action>
  <verify>
    Inspect the UI elements using the Accessibility Inspector (if possible) or verify the code adds the correct modifiers.
  </verify>
  <done>
    The app is more usable for individuals using VoiceOver, with clear labels and actions for key interactive elements.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundaries introduced.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Accessibility Labels | accept | Labels only contain public station names and price categories (RAG), which are already visible. |
</threat_model>

<verification>
Check for the presence of ProgressView when refreshing, verify pull-to-refresh works on all lists, and ensure accessibility labels are present in the source code.
</verification>

<success_criteria>
1. Loading feedback is visible during all data refreshes.
2. Color contrast for "Average" stations meets readability standards.
3. Pull-to-refresh is functional on both station and history lists.
4. Favorite buttons and map markers have descriptive accessibility properties.
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-polish/05-02-SUMMARY.md`
</output>
