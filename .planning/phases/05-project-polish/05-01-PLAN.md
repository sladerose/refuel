---
phase: 05-project-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [refuel/StationDetailView.swift, refuel/StationListView.swift, refuel/MapView.swift, .planning/STATE.md, .planning/ROADMAP.md]
autonomous: true
requirements: [INFO-04, INFO-03]

must_haves:
  truths:
    - "User can view opening hours and services for a station"
    - "User can trigger external navigation from both map and list views"
    - "All Phase 1-4 features are functional and bug-free"
  artifacts:
    - path: "refuel/StationDetailView.swift"
      provides: "Detailed station info and navigation links"
  key_links:
    - from: "refuel/StationListView.swift"
      to: "refuel/StationDetailView.swift"
      via: "NavigationLink"
    - from: "refuel/MapView.swift"
      to: "refuel/StationDetailView.swift"
      via: ".sheet"
    - from: "refuel/StationDetailView.swift"
      to: "refuel/NavigationService.swift"
      via: "NavigationService.shared.open"
---

<objective>
Polish the application by addressing missing UI wires and feature gaps identified during verification. Specifically, integrate navigation services, add detailed station info (hours/services), and perform a final verification of all requirements from Phases 1-4.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@refuel/Models.swift
@refuel/NavigationService.swift
@refuel/StationListView.swift
@refuel/MapView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StationDetailView</name>
  <files>refuel/StationDetailView.swift</files>
  <action>
    Create a SwiftUI view `StationDetailView` that:
    - Displays station name, address, and RAG-coded price summary.
    - Shows `openingHours` (if available).
    - Lists `services` using a chip-based layout (if available).
    - Lists all `FuelPrice` grades and prices.
    - Includes "Navigate in Apple Maps" and "Navigate in Google Maps" buttons using `NavigationService.shared.open`.
    - Shows "Add to Favorites" toggle.
  </action>
  <verify>
    `StationDetailView.swift` exists and compiles.
  </verify>
  <done>
    Detailed station information and navigation buttons are presented in a dedicated view.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire StationDetailView into Main UI</name>
  <files>refuel/StationListView.swift, refuel/MapView.swift</files>
  <action>
    - In `StationListView.swift`: Wrap `StationRow` in a `NavigationLink` pointing to `StationDetailView(station: station)`.
    - In `MapView.swift`: 
        - Add a `@State private var selectedStation: Station?`
        - Trigger `selectedStation = station` when a station marker is tapped.
        - Add a `.sheet(item: $selectedStation)` to display `StationDetailView(station: selectedStation)`.
  </action>
  <verify>
    Tapping a station in the list or map opens the detailed view.
  </verify>
  <done>
    Users can access station details from both primary discovery interfaces.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final Verification and Documentation Update</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    - Perform a manual check of all core features:
        - RAG status color coding on map/list.
        - Search location updates the map.
        - Geofence-triggered refreshes (mock via geofence exit simulation).
        - Favorite management.
        - Refuel history logging and spend calculation.
    - Update `ROADMAP.md`: Mark Phase 5 as completed and verify all prior phases are correctly marked.
    - Update `STATE.md`: Update `Current Position` to Phase 5 complete, clear any TODOs, and update `Next Steps`.
  </action>
  <verify>
    `ROADMAP.md` and `STATE.md` reflect the final polished state of the project.
  </verify>
  <done>
    Project is documented as complete and ready for release.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → External Maps | Data passed to Apple/Google Maps URL schemes (coordinate, label) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | NavigationService | mitigate | Use URL-encoded labels and coordinate-based navigation to ensure valid URL schemes. |
</threat_model>

<verification>
Check that all station details are visible and navigation buttons correctly attempt to open their respective map apps.
</verification>

<success_criteria>
1. User can see opening hours and services for any fuel station.
2. User can navigate to a station from the app using their choice of Apple or Google Maps.
3. ROADMAP.md and STATE.md accurately reflect the project completion status.
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-polish/05-01-SUMMARY.md`
</output>
