---
phase: 04-personalization-cost-tracking
plan: 01
type: execute
wave: 1
depends_on: [03]
files_modified: [refuel/Models.swift, refuel/StationListView.swift, refuel/MapView.swift, refuel/ContentView.swift]
autonomous: true
requirements: [TRACK-02]
must_haves:
  truths:
    - "User can toggle a 'Favorite' status for any station."
    - "User can view a filtered list of their favorite stations."
    - "Favorite status is persisted across app launches via SwiftData."
  artifacts:
    - path: "refuel/Models.swift"
      provides: "Updated Station model with isFavorite property."
---

<objective>
Implement Favorites management to allow users to save and quickly access their preferred fuel stations.

Purpose: Provide personalized access to frequently visited stations (TRACK-02).
Output: Updated Station model, favorite toggles in UI, and a dedicated Favorites tab/view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@refuel/Models.swift
@refuel/StationListView.swift
@refuel/MapView.swift
@refuel/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Station model for Favorites</name>
  <files>refuel/Models.swift</files>
  <action>
    - Add `var isFavorite: Bool = false` to the `Station` model.
    - Update the `init` to include the new property.
  </action>
  <done>Station model supports favorite status persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Favorite Toggle in UI</name>
  <files>refuel/StationListView.swift, refuel/MapView.swift</files>
  <action>
    - Add a "heart" button to `StationRow` in `StationListView.swift` to toggle `isFavorite`.
    - Add a "heart" icon or toggle to the station detail/annotation in `MapView.swift`.
  </action>
  <done>Users can toggle favorite status from map and list views.</done>
</task>

<task type="auto">
  <name>Task 3: Add Favorites Tab/Filter</name>
  <files>refuel/ContentView.swift, refuel/StationListView.swift</files>
  <action>
    - Update `ContentView.swift` to include a new "Favorites" tab.
    - Create or reuse `StationListView` with a filter for `isFavorite == true`.
  </action>
  <done>Users have a dedicated view for their favorite stations.</done>
</task>

</tasks>

<success_criteria>
- Station model persists `isFavorite` status.
- UI allows toggling favorite status with immediate visual feedback.
- A dedicated view displays only favorite stations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-personalization-cost-tracking/04-01-SUMMARY.md`
</output>
