---
phase: 04-personalization-cost-tracking
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [refuel/Models.swift, refuel/RefuelLogView.swift, refuel/ContentView.swift]
autonomous: true
requirements: [TRACK-01]
must_haves:
  truths:
    - "User can log individual refuel events with date, amount, price, and station."
    - "User can view a historical list of refuel events."
    - "Refuel logs are persisted via SwiftData."
  artifacts:
    - path: "refuel/Models.swift"
      provides: "New RefuelEvent model."
    - path: "refuel/RefuelLogView.swift"
      provides: "UI for viewing and adding refuel logs."
---

<objective>
Implement cost tracking to allow users to monitor their fuel spending and history.

Purpose: Help users track their fuel costs and consumption patterns (TRACK-01).
Output: New RefuelEvent model, a dedicated log view, and purchase entry UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@refuel/Models.swift
@refuel/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RefuelEvent model</name>
  <files>refuel/Models.swift</files>
  <action>
    - Add `@Model final class RefuelEvent` with:
      - `id: UUID`
      - `date: Date`
      - `amountInLitres: Double`
      - `pricePerLitre: Double`
      - `grade: String`
      - `stationName: String`
      - `station: Station?` (optional relationship)
    - Update `Station` model to include a relationship to `RefuelEvent`.
  </action>
  <done>Data model for refuel history is defined and persistent.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Refuel Log UI</name>
  <files>refuel/RefuelLogView.swift</files>
  <action>
    - Create `RefuelLogView.swift` to display a list of all `RefuelEvent` objects.
    - Implement a form to add a new `RefuelEvent`.
    - Show summary stats (e.g., total spend) at the top of the history view.
  </action>
  <done>User can view history and log new fuel purchases.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Main Navigation</name>
  <files>refuel/ContentView.swift</files>
  <action>
    - Add an "Activity" or "History" tab to `ContentView.swift`.
    - Ensure new refuel logs can be easily added from the UI (e.g., a "plus" button).
  </action>
  <done>Cost tracking is accessible from the main navigation.</done>
</task>

</tasks>

<success_criteria>
- RefuelEvent model persists refuel data correctly.
- History view displays all logged events in reverse chronological order.
- Summary statistics reflect the total spent across all events.
</success_criteria>

<output>
After completion, create `.planning/phases/04-personalization-cost-tracking/04-02-SUMMARY.md`
</output>
