---
phase: 09-rewards-social
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [refuel/Models.swift, refuel/GamificationManager.swift, refuel/ProfileView.swift, refuel/PriceVerificationView.swift, refuel/ContentView.swift]
autonomous: true
requirements: [REWARDS-01, REWARDS-02, SOCIAL-01]
must_haves:
  truths:
    - "Scanned receipts/verifications automatically create LotteryEntry records."
    - "Profile view displays current month lottery entries."
    - "Users can generate and share a 'Savings Achievement' card."
    - "A 'Global Impact' counter animates in the Profile view."
  artifacts:
    - path: "refuel/Models.swift"
      provides: "LotteryEntry model"
    - path: "refuel/ProfileView.swift"
      provides: "Social Hub and Community Dashboard UI"
  key_links:
    - from: "refuel/GamificationManager.swift"
      to: "refuel/Models.swift"
      via: "LotteryEntry creation"
---

<objective>
Close the engagement loop by implementing tangible reward mechanisms (lottery) and social proof (sharing/community stats).

Purpose: Solidify the 'habit' by rewarding contributions and showing users they are part of a larger, impactful mission.
Output: Lottery system, social sharing hub, and community impact dashboard.
</objective>

<execution_context>
@refuel/Models.swift
@refuel/GamificationManager.swift
@refuel/ProfileView.swift
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-engagement-engine/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lottery System & Persistence</name>
  <files>refuel/Models.swift, refuel/refuelApp.swift, refuel/GamificationManager.swift</files>
  <action>
    - Add `LotteryEntry` model to `Models.swift`: `id`, `date`, `stationName`, `contributionType` (enum: scan, verify).
    - Update `refuelApp.swift` schema to include `LotteryEntry`.
    - In `GamificationManager`, add `createLotteryEntry(type: ContributionType, stationName: String)`.
    - Update `awardXP` to also trigger `createLotteryEntry`.
  </action>
  <verify>
    <automated>Verify LotteryEntry records exist in SwiftData after a mock scan/verify action.</automated>
  </verify>
  <done>Every valid contribution earns the user a trackable entry into the monthly lottery.</done>
</task>

<task type="auto">
  <name>Task 2: Social Sharing - Achievement Cards</name>
  <files>refuel/ProfileView.swift, refuel/AchievementCardView.swift</files>
  <action>
    - Create `AchievementCardView.swift` as a stylized SwiftUI view designed for snapshotting (e.g., 1080x1080).
    - Display monthly savings, current rank, and "Fuel Scout" branding.
    - In `ProfileView`, add a "Share My Impact" button that uses `ImageRenderer` (iOS 16+) to generate a UIImage from the card.
    - Present the system `ShareSheet`.
  </action>
  <verify>
    <automated>UI check: Share sheet appears with a generated image when button is tapped.</automated>
  </verify>
  <done>Users can broadcast their savings and rank to social media, driving organic growth.</done>
</task>

<task type="auto">
  <name>Task 3: Community Dashboard & Global Impact</name>
  <files>refuel/ProfileView.swift, refuel/GamificationManager.swift</files>
  <action>
    - In `GamificationManager`, implement a `globalImpactTotal` (mocked initially by locally aggregating all users' verifications * 10).
    - In `ProfileView`, add a prominent "Fuel Scout Network" section.
    - Implement an animating digit counter for the "Total Community Savings" (e.g., "R1,420,560 saved this year").
    - Display the user's lottery entry count for the current month.
  </action>
  <verify>
    <automated>UI check: Impact counter animates on Profile view appearance.</automated>
  </verify>
  <done>Profile view serves as a powerful rewards hub and social proof center.</done>
</task>

</tasks>

<success_criteria>
- Contributions automatically generate lottery entries.
- Profile view shows entry count and animated community savings.
- 'Share Impact' button generates a high-quality branded card for social sharing.
- App-wide gamification loop is closed and polished.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rewards-social/09-01-SUMMARY.md`
</output>
